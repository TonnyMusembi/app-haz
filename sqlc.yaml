version: "2"
sql:
  - schema: "./db/schema.sql"
    queries: "./db/queries.sql"
    engine: "mysql"
    gen:
      go:
        package: "db"
        out: "./db/sqlc"
        emit_json_tags: true
        emit_prepared_queries: true